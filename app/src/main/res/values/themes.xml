<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- 🔹 Motyw główny aplikacji -->
    <style name="Theme.QuestMapGPS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Twoje kolory, style itp -->
    </style>

    <!-- 🔹 Motyw ekranu startowego -->
    <style name="Theme.QuestMapGPS.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.QuestMapGPS</item>
    </style>

</resources>
